# Node / React
node_modules/                      # [dependencies cached locally; don't commit]
frontend/node_modules/             # [frontend-specific node_modules]
backend/node_modules/              # [backend-specific node_modules]

# Build outputs
frontend/build/                    # [React production build]
dist/                              # [general distribution folder]

# Docker
*.tar                              # [avoid large image tarballs]
docker-compose.override.yml        # [local overrides that shouldn't be committed]

# System files
.DS_Store                          # [macOS file system metadata]
.env                                # [local env files with secrets - never commit]
